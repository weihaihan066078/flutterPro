import 'package:flutter/material.dart';

class MinePages extends StatelessWidget {
  const MinePages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('text page'),
    );
  }
}